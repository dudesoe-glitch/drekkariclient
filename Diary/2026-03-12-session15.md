# Session 15 — 2026-03-12

## What We Did
- Fixed minimap click-to-move: added cross-segment fallback using dloc + player.rc offset
- Relaxed MapWnd.clickloc segment check to pass through to improved mvclick handler
- Created CombatRotationBot: configurable skill sequence executor with loop/cooldown options
- Updated weapon distances with Ring of Brodgar wiki range multipliers (BASE_MELEE_DIST * multiplier)
- Added 10+ new weapons to distance table (knives, scythe, sling, metal axe, etc.)
- Fixed CombatDistanceTool double-destroy bug (stop() called destroy() + wdgmsg called reqdestroy())
- Added null safety to CombatDistanceTool.stop() for gui.map
- Added thread safety to CombatRotationBot (synchronized steps list)
- Added slot key reference label to CombatRotationBot UI
- Researched Ardennes and Amber H&H clients (background)
- Ran full bot audit (background)

## Texture
- Minimap fix was deep investigation: traced entire click path through MiniMap→MapWnd→mvclick→wdgmsg
- Root cause: sessloc segment check silently drops clicks when segments differ or sessloc is null
- Fix uses dloc (display center) + player.rc for cross-segment coordinate computation
- Combat rotation uses Fightsess wdgmsg("use"/"rel") protocol with cooldown tracking
- Weapon distances now use formula: BASE_MELEE_DIST (13.5) * wiki range multiplier (1.0-1.6)
- User pointed to Ring of Brodgar wiki as source of truth for weapon ranges

## Key Decisions
- CombatRotationBot doesn't extend BotBase — combat tools have different lifecycle (no vitals checking)
- Weapon distances use multiplier-based formula so user can tune one constant (BASE_MELEE_DIST)
- CombatRotationBot uses Collections.synchronizedList for thread-safe step access
- Minimap fix is backwards-compatible: same-segment clicks behave exactly as before
- .res file for CombatRotation created by binary-patching CombatDistanceTool.res (same icon, padded names)

## Stats
- 8 files modified, 3 new files
- Build clean

## For Next Time
- Test all changes in-game (now 15 sessions!)
- Tune BASE_MELEE_DIST with in-game testing
- Review Ardennes/Amber client research results
- Apply bot audit fixes
- Consider migrating CombatDistanceTool to BotBase pattern
