# Session 1 — 2026-03-11

## What We Did
- First real coding session after Session 0 (setup only)
- Installed Apache Ant 1.10.15 via manual download (winget didn't have it)
- Implemented all 4 priority features: inventory sort, farming bot, seed quality picker, item filter
- Ran 3 parallel background agents: code audit, farming research, ItemFilter research
- Applied 5 bug fixes from audit findings (savewndpos brace, OceanScout precedence, rstate index, parseInt crash, sort safety drop)
- Fixed build encoding (UTF-8) which resolved 27 pre-existing unicode compile errors

## Texture
- User wants to move fast — said "lets work on all the priorities" and "we should also run audits"
- Launched research agents in background while coding features in parallel — good throughput
- The farming bot research revealed my initial planting mechanic was wrong (`place` vs `itemact`) — caught and fixed before commit
- The code audit was very productive — found real bugs that affect users (window positions not saving, walrus distance always triggering)
- 5 pre-existing compile errors in FishingBot/GobCombatDataInfo remain — not our code, likely missing dependencies or upstream changes

## Key Decisions
- Inventory sort uses swap-chain algorithm (like EnderWiggin) rather than simpler but slower approach
- FarmingBot includes seed quality picker directly rather than as a separate tool
- ItemFilter kept simple for now (quality only) — FEP/armor/curiosity filters deferred
- Fixed upstream WItem.rstate bug (ols.get(0) → ols.get(i)) even though it's not our code

## For Next Time
- User requested **custom map marker images** — wants to mark clay nodes etc. with item icons via dropdown+search. Need to research existing marker code first (MapFile/MapView/MapWnd area)
- Should fix the 5 pre-existing compile errors
- Audit backlog: InterruptedException swallowing, spr() side effects, OptWnd god class, wmap thread safety
- User prefers working in priority order and parallelizing research
