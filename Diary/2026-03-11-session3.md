# Session 3 — 2026-03-11

## What We Did
- Verified research document features against existing codebase (7 parallel agents)
- Found 3 features already existed (minesweeper, drink refill, parasite auto-drop)
- Implemented 5 new features, all compile clean
- Updated research memory to reflect verified state

## Features Built
1. Equipment quick-swap hotkeys — 17 KeyBindings for EquipFromBelt actions
2. Per-item auto-drop — ItemAutoDrop + ItemAutoDropWindow, JSON config in prefs
3. Batch crafting "Craft N" — TextEntry + button + daemon thread in Makewindow
4. Expanded ItemFilter — FEP, armor, LP/LPH, container contents, wear filters
5. Inventory toolbar — Sort button, Search button, live item count in inventory window

## Texture
- User said "work in priority order" + referenced research doc
- Verification step was critical — saved time by not reimplementing 3 existing features
- Session was very efficient: 5 features + 3 verifications + memory updates in one session
- All builds passed first try — no compile errors this session
- Per-item auto-drop designed to coexist with existing category-based system (not replace)
- Craft N uses 650ms delay between crafts — may need tuning based on server response time

## Key Decisions
- Equipment hotkeys all default to KeyMatch.nil (unbound) — users set them in Options
- Per-item auto-drop checks BEFORE category auto-drop (per-item takes priority)
- ItemAutoDrop uses ConcurrentHashMap for thread safety
- Craft N is daemon thread so it doesn't block client shutdown
- ItemFilter refactored with centralized compare() helper and ALL_PATTERNS array
- Inventory toolbar uses tick() for live item count (simple, works across all inventory sizes)
- Inventory toolbar positioned at y=0 with inventory shifted to y=22 (compact)

## For Next Time
- Test all features in-game (especially Craft N timing and equipment swap with belt items)
- Batch animal butchering bot (next TIER 1 remaining from research)
- Flat terrain toggle, FEP modifier tooltips (TIER 2 features)
- Consider inventory grouping modes (By Name, By Quality brackets)
- OptWnd refactor is overdue but low urgency
